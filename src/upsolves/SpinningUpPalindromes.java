package upsolves;
import java.util.*;

public class SpinningUpPalindromes {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		char[] s = sc.nextLine().toCharArray();
		sc.close();
		int n = s.length;
		for(int i = 0; i < Math.pow(10, n / 2 + 1); i++) {
			
		}
	}
}
